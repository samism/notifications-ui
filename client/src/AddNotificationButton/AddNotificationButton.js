import React from 'react';
import StyledButton from '../StyledButton';

const AddNotificationButton = () => {
  return <StyledButton>Add Notification</StyledButton>;
};

export default AddNotificationButton;
